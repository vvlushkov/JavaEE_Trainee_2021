First command in terminal:
    docker-compose up --build

After building and starting of docker-compose paste this link in browser:
    http://localhost:8080/authorization

First two users:
    LOGIN   PASSWORD
    ----------------
    admin - password
    user  - password

In file jdbc.properties you can switch db.url and work with local server.